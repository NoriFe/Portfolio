{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "http://localhost:3000/",
  "main": "index.js",
  "scripts": {
    "publish": "npm run publish",
    "sass": "npx node-sass -w src/scss/main.scss main.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mime": "^3.0.0",
    "normalize.css": "^8.0.1",
    "react": "^18.2.0"
  },
  "devDependencies": {
    "node-sass": "^9.0.0"
  }
}
